const userControllers = require("./user-controllers");
const chatControllers = require("./chat-controllers");
const messageControllers = require("./message-controller");

module.exports = { userControllers, chatControllers, messageControllers };
